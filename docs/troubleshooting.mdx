---
sidebar_position: 5
title: ⚠️ Troubleshooting
---
import trustStyles from '@site/src/components/TrustRankRequirement.module.css';
import { HoverOption, HoverVideo } from '@site/src/components/HoverOption';
import { DocsMediaRow } from '@site/src/components/DocsMediaRow';

# Common Errors

A reference for errors you're likely to run into when uploading or using avatars.

---

## SDK / Upload errors

### "No avatar descriptor found"

**Cause:** The selected object doesn't have a `VRC Avatar Descriptor` component, or no object is selected in the Builder tab.

**Fix:** Select the root GameObject of your avatar in the scene, then open the SDK Builder tab again. Make sure a `VRC Avatar Descriptor` is attached to that object.

---

### "Build failed" with no clear reason

**Cause:** Often a script compilation error somewhere in the project that prevents Unity from building.

**Fix:**
1. Check the Unity **Console** (Window → General → Console) for any red errors.
2. Fix all script errors before trying to upload.
3. If errors are in third-party packages, try reimporting them via the Creator Companion.

---

### "VRCFury encountered an error."
<SubsectionBanner />

<div>
    <div>
    If you get this error while entering play mode or attempting to upload the model that means you are missing either **GoGoLoco** or **ADJerry's Facetracking Templates** while using an older version of the Avatar. <br />
    Please either update the Lucario Avatar or remove "FT- Lucario ReUnited UE FaceTracking Prefab" or "GogoLoco Beyond - Lucario ReUnited  (VRCFury)" from the avatar in the Hierarchy. <br />
    Compare your error to the images below to know which one to delete.
    </div>
    <div style={{ display: "flex", alignItems: "flex-start", gap: "1rem", flexWrap: "nowrap" }}>
        <LocationCard src="/img/troubleshooting/gogoloco-error.png" title="GoGoLoco Error" style={{ flex: "0 1 auto", minWidth: 0 }} />
        <LocationCard src="/img/troubleshooting/vrcft-error.png" title="VRCFT Error" style={{ flex: "0 1 auto", minWidth: 0 }} imageHeight="301px" />
        [The 1px is needed for the frames to align]: #
    </div>
</div>

---

## Unity errors

### Missing scripts on avatar components

{/* Add details here */}

---

### Avatar shows as bright neon pink
<SubsectionBanner />

<div className={trustStyles.row}>
    <div className={trustStyles.content}>
        If your avatar appears as neon pink you most likely are missing the Poiyomi Shaders! <br />
        In order to fix you will need to install Poiyomi (Follow this [guide](./upload/installation#step-4---add-the-package-sources-to-vcc))
    </div>
    <div className={trustStyles.media}>
        <LocationCard src="/img/troubleshooting/shader-error.png"/>
    </div>
</div>

<HoverOption>
<DocsMediaRow media={<HoverVideo src="/vid/troubleshooting/fixShadersGuide.mp4" title="Fixing Shaders" resetOnHover="true"/>}>
Test
</DocsMediaRow>
</HoverOption>

---

## Quest-specific issues

### Quest avatar shows as a different avatar than PC

**Cause:** The Quest and PC versions have different Pipeline IDs and aren't linked.

**Fix:** See the [Quest version linking steps](upload/uploading#5-upload-mobile-version) in the Uploading guide.

---

:::info
Don't see your error here? More entries will be added over time. Feel free to reach out if you're stuck.
:::
